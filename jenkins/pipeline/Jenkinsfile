pipeline {
    agent any
    
    stages {
        
        stage('Terraform') {

            agent {
                dockerfile {
                    filename 'Dockerfile'
                    dir 'jenkins/terraform-slave'
                    args '-v ${context.WORKSPACE}:/var/git'
                }
            }
            
            steps {
                sh '/bin/sh echo 123'
            }
            
        }
        
    }
}